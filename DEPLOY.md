# GitHub Pages部署指南

## 🚀 自动部署到GitHub Pages

### 方法1：使用GitHub Actions（推荐）

项目已配置好GitHub Actions工作流，当您推送代码到main分支时，会自动构建并部署到GitHub Pages。

#### 步骤：

1. **启用GitHub Pages**：
   - 进入您的GitHub仓库
   - 点击 **Settings** 选项卡
   - 在左侧菜单中找到 **Pages**
   - 在 "Build and deployment" 部分的 "Source" 下，选择 **GitHub Actions**

2. **触发部署**：
   - 推送代码到main分支：
   ```bash
   git add .
   git commit -m "添加GitHub Pages部署配置"
   git push origin main
   ```

3. **查看部署状态**：
   - 进入 **Actions** 选项卡查看构建进度
   - 构建完成后，您的网站将在：`https://[您的用户名].github.io/FinTrack`

### 方法2：手动部署

如果您想手动控制部署时机：

1. **构建并部署**：
   ```bash
   npm run deploy
   ```

2. **启用GitHub Pages**：
   - 进入仓库的 **Settings** → **Pages**
   - Source 选择 **Deploy from a branch**
   - Branch 选择 **gh-pages** 文件夹
   - 保存设置

### 📁 部署配置说明

- **`.github/workflows/deploy.yml`**: GitHub Actions工作流配置
- **`package.json`**: 添加了部署脚本和gh-pages依赖
- **`public/manifest.json`**: PWA配置，支持离线访问

### 🔧 技术细节

- 使用React的`build`脚本生成静态文件
- 通过`gh-pages`包将build目录推送到gh-pages分支
- GitHub Pages从gh-pages分支提供静态网站服务

### 📱 功能特性

✅ 响应式设计，支持移动端和桌面端  
✅ 现代化UI设计，专业的财务管理界面  
✅ 收入/支出记录和管理  
✅ 实时数据统计和图表分析  
✅ 数据导入/导出功能（CSV格式）  
✅ PWA支持，可安装到主屏幕  

### 🌐 部署后访问

部署完成后，您可以通过以下URL访问应用：
`https://[您的GitHub用户名].github.io/FinTrack`